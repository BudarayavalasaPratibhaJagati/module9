# Module 9 — FastAPI + PostgreSQL + pgAdmin (Docker Compose)

A tiny, reproducible stack to practice SQL CRUD with a FastAPI app backed by PostgreSQL, with pgAdmin for running queries and screenshots.

---

## 🧰 Tech
- **FastAPI** (Python 3.11, Uvicorn)
- **PostgreSQL 16**
- **pgAdmin 4**
- **Docker Compose**

---

## ⚙️ Quick Start

```bash
# start everything (builds on first run)
docker compose up -d --build
